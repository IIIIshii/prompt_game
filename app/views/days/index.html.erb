<h1>ゲーム一覧</h1>

<%= button_to "＋ 新しいゲームを作る", days_path, method: :post, style: "padding: 10px; font-size: 16px; cursor: pointer;" %>

<hr>

<ul>
  <% @days.each do |day| %>
    <li style="margin-bottom: 10px;">
      <strong>Day ID: <%= day.id %></strong> 
      ( <%= day.created_at.strftime('%Y/%m/%d %H:%M') %> )
      <br>
      <%= link_to "📜 ログを見る", day_path(day) %>
      
      | 🎮 プレイURL: 
      <code><%= entry_day_url(day, pass: "パスワード") %></code>
    </li>
  <% end %>
</ul>